export function syncUsers(state, data) {
  state.users = data;
}

export function syncGroups(state, data) {
  state.groups = data;
}

export function syncMenu(state, data) {
  state.menu = data;
}

export function syncSettings(state, data) {
  state.settings = data;
}
